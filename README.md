# pjava

Copyright (c) 2025 Ronan MERIEN

This is a Pseudo Java compiler in Java for studies purposes.

gram\_pjava.dat is the grammar of the language use by gbf program (grammar compiler) to compile into tab\_pjava.dat (binary version) use by the Pseudo Java compiler via the Loader class.

Use the main class to compile from prgs/\*.pjava to obj files (text) you can run with the imp "Interpreteur pour Machine à Pile" (stack machine interpreter in english).





